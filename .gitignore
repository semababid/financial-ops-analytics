# Data. Raw CSVs (~120MB) and the intermediate tables (~34MB) are rebuildable
# from Kaggle, so neither is committed.
data/raw/
data/processed/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# OS / editor / tooling
.DS_Store
.idea/
.vscode/
.claude/

# data/dashboard/ is deliberately NOT ignored: ~40KB of pre-aggregated tables so
# the deployed Streamlit app has data without the raw CSVs (see
# src/dashboard_data.py). The bulky parquet lives under data/processed/, which
# is excluded by directory above.
